backendResources:
  cpu: 3            # Must be within [1, 8]. Default is 1.
  memory: 1024      # Must be within [512, 32768]. Default is 512.
  maxInstances: 4   # Must be within [1, 1000]. Default is 100.
  concurrency: 100  # Must be within [1, 100]. Default is 80.